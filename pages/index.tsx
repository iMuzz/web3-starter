import type { NextPage } from 'next'
import { Toaster } from 'react-hot-toast'
import Head from 'next/head'

import Header from '../components/header'
import { data } from '../ethers'
import ControlPanel from '../components/controlPanel'

const Home: NextPage = () => {
  const { gameState, soldOut, etherPrice } = data.useContainer() // Global data store

  return (
    <div>
      <div className="max-w-screen-xl m-auto">
        <Head>
          <title>Web 3 Starter Project</title>
          <meta name="description" content="Generated by create next app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>
        <Header />
        <Toaster position="top-left" />

        <h1 className="m-auto text-center text-7xl"> Create Web 3 App</h1>
        <div className="flex justify-center mt-12">
          <ControlPanel />
        </div>

        <div className="flex-1 px-4 md:px-12 flex flex-col justify-center items-center">
          <div className="flex flex-col font-game text-xl text-center"></div>
        </div>
        {/* Give user instructions to play the game */}
      </div>
    </div>
  )
}

export default Home
